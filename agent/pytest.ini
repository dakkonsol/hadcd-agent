[pytest]
# Run inside the agent container:
#   docker compose run --rm --entrypoint pytest agent tests/
# The agent package lives one directory up at /app/agent; pytest must
# be able to import it.
pythonpath = ..
testpaths = tests
asyncio_mode = auto
